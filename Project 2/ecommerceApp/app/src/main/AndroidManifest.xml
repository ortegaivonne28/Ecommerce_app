<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.ivonneortega.ecommerceapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailView.DetailViewActivity"
            android:label="@string/title_activity_detail_view"
            android:theme="@style/ProductViewTheme">
        </activity>
        <activity
            android:name=".CartActivity.CartViewActivity"
            android:label="@string/title_activity_cart_view"
            android:theme="@style/CartViewTheme">
        </activity>
        <activity
            android:name=".Wishlist.WishlistActivity"
            android:label="@string/title_activity_wishlist"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>